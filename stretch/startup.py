from django.conf import settings


def run():
    pass
    '''
    source = settings.STRETCH['SOURCE']
    backend = settings.STRETCH['BACKEND']
    if source:
        if source.iss AutoloadableSource:
            source.on_filesystem_change = backend.push
            fork source.monitor()
        else:
            
    else:
        raise Undefined Source
    '''
